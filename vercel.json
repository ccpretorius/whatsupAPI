{
  "version": 2,
  "builds": [
    {
      "src": "backend/server.mjs",
      "use": "@vercel/node"
    },
    {
      "src": "src",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/server.mjs"
    },
    {
      "src": "/(.*)",
      "dest": "src/dist/$1"
    }
  ]
}
